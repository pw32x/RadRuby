<UserControl x:Class="SceneMaster.Scenes.Views.ScenePropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:pt="http://propertytools.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
        <pt:PropertyGrid SelectedObject="{Binding Scene.ForegroundTiledMap}"
                         TabVisibility="Collapsed" />
        <pt:PropertyGrid SelectedObject="{Binding Scene.BackgroundTiledMap}"
                         TabVisibility="Collapsed" />            
        </StackPanel>
    </Grid>
</UserControl>
